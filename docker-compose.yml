services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: SISIII2026_89241167
      POSTGRES_USER: studenti
      POSTGRES_PASSWORD: S039C8R7
    ports:
      - "5433:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./backend/drizzle/0000_wise_shen.sql:/docker-entrypoint-initdb.d/0000_wise_shen.sql

volumes:
  db_data:
